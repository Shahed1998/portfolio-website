const Project = (props) => {
  return (
    <div
      ref={props.view}
      className='container min-vh-100'
      style={{ backgroundColor: 'orange' }}
    >
      Project
    </div>
  );
};

export default Project;
