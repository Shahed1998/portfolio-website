const Contact = (props) => {
  return (
    <div
      ref={props.view}
      className='container min-vh-100'
      style={{ backgroundColor: 'blue' }}
    >
      Contact
    </div>
  );
};

export default Contact;
