const Skill = (props) => {
  return (
    <div
      ref={props.view}
      className='container min-vh-100'
      style={{ backgroundColor: 'green' }}
    >
      Skill
    </div>
  );
};

export default Skill;
